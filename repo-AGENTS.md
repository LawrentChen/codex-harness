# 项目规则

## Python 环境

- 本项目默认使用 Conda 环境 ``。
- 运行 Python、测试、格式化、静态检查或项目脚本前，先激活该环境。

## 项目文档

本项目使用以下文档分别描述业务、内部实现和对外契约。三者应各司其职，避免重复维护同一细节：

- `docs/BUSINESS.md`：回答“为什么做、结果应当是什么”。记录业务目标、领域对象、术语、计算口径与公式、业务规则、用户可见能力等。内容应尽量独立于具体代码结构；涉及实现时只说明会影响业务结果的约束。
- `docs/ARCHITECTURE.md`：回答“内部如何实现和运行”。记录目录与分层、模块职责、依赖方向、数据流、运行时流程、配置、缓存与存储路径、技术选型及其约束；需要时使用 Mermaid 图。不要在此重复完整业务公式或逐项 API 参数说明。
- `docs/API.md`：回答“外部如何稳定地调用和依赖”。记录项目公开入口以及跨模块稳定边界的契约，包括导入路径、函数或类签名、参数与返回值语义、输入输出结构、空数据与异常约定、副作用和最小调用示例。内部辅助函数和随实现变化的细节不属于 API。

若项目以一个完整流程对外提供能力，例如因子生产、组合优化或批处理任务，而没有多个独立公开 API，则 `API.md` 只需说明核心入口、调用方式、输入输出契约和运行副作用，不必为了填充文档而罗列内部函数。若项目有多个可被其它项目分别导入和依赖的模块，则按公开模块组织其契约。

### 阅读顺序

首次进入项目、开始跨模块改动或需要建立整体理解时，按以下顺序通读：

1. `docs/BUSINESS.md`
2. `docs/ARCHITECTURE.md`
3. `docs/API.md`

修改前按改动范围阅读，不要求每次全文通读三份文档：

- 涉及业务口径、用户可见指标、术语、文案、公式或结果解释时，先读 `docs/BUSINESS.md` 及其中相关外链。
- 涉及目录结构、数据流、新页面或新包、运行流程、缓存、配置、存储路径或依赖方向时，读 `docs/ARCHITECTURE.md`。
- 涉及公开函数或类的签名、入参出参语义、入口调用方式、异常与空数据约定，或其它模块依赖的边界时，读 `docs/API.md`。
- 对错别字、注释等明显局部修改，或不改变业务口径和边界的单一函数内部修复，至少检查与改动最相关的一份文档。

### 修改责任

- 修改后检查本次改动是否改变了业务行为、公开契约、模块职责、数据流、配置或路径；如有变化，同步更新对应文档。
- 同一变化可能需要更新多份文档，但应从各文档自身视角描述，不要复制同一段实现说明。
- 提交前核对三份文档中与本次改动相关的段落仍与代码和测试一致。
- 发现文档与代码不一致时，先核实预期行为，判断是代码缺陷还是文档过期；不要未经判断就以其中一方覆盖另一方。确认当前代码行为正确后，再更新文档。

## README 定位

根目录 `README.md` 面向项目使用者，主要包含项目概述、安装与环境要求、快速开始和常见基本用法。深入的业务口径、内部架构和完整公开契约分别链接到 `docs/BUSINESS.md`、`docs/ARCHITECTURE.md` 和 `docs/API.md`，避免在 README 中重复维护大段细节。